# OliviaSoul v18 资产说明

本目录来自 `yilangren/OliviaSoul` 的 v18 Harness，来源提交为 `2ffe7f1c2f73d0c3b00c25258e0ce93b8f4b92ad`。

仓库根目录未发现 LICENSE 文件，因此这些文件仅作为当前用户已取得的本地开发资产保留；在公开发行包中继续分发前，必须取得作者许可或改为运行时由用户提供。不得把 API Key、真实信件、`.cursor/secrets/`、`_probe/` 或运行时数据库放入这里。

适配器入口是 `src/letters/model-adapter.js` 的 `OliviaSoulHarnessProvider`。
