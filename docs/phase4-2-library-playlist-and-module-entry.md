# Phase 4-2：曲库、歌单和模块选择入口

## 目标

让用户上传的曲目稳定出现在“我的上传”曲库，并能按原版接口加入或移出歌单；同时让用户能够看懂当前选择的播放器、渲染器和编码器，并通过 `config/user-config.json` 选择可用实现。

原生接管问题 `LINLI-PLAY-001` 的定位和修复由 Phase 4-3/4-4 记录；本里程碑复用其曲目与媒体契约，并增加经过核查的前端试听地址补丁，不修改 DLL，不实现视频回信或 3D 演奏生成。模块选择只允许注册表中已有的实现，不能把某一个实现写死成唯一实现。

状态：**2026-09-11 收尾验收通过。** 用户已确认歌单名称、加入/去重/移除、歌单演奏、预设元数据/封面回退和最终 localhost 版本的试听正常；配置选择、重启生效及媒体格式契约通过自动化验证。README 的 Phase 4-2 可勾选，完整发行安装流程仍按后文边界跟踪。

## 当前实现边界

- `/toy/searchUserSongs` 只返回已完成的 MIDI 任务，支持分页和原版曲目字段；失败、取消和处理中任务不会进入曲库。
- `/toy/searchPlaylist`、`/toy/addToPlaylist` 和 `/toy/delFromPlaylist` 提供原版歌单读取、加入和移除；SQLite 是歌单事实源，重复加入按 `(itemType, itemId)` 幂等处理。
- 原版客户端加入歌单时通常只提交 `itemType/itemId`；对于已完成的本地 MIDI，网关会通过现有曲目适配器补齐名称、媒体 URL、TOD 视图、时长和演奏类型，并把 `createdAt` 按原版要求返回 Unix 秒数。历史上只保存 ID 的歌单记录在再次加入时也会被补齐。
- `config/user-config.json` 的 `music.renderer`、`music.playbackAdapter` 和 `music.encoder` 会在本地服务启动时经过注册表校验，并装配到 MIDI 任务服务。
- `music.encoder` 的扩展名和 MIME 契约继续由编码器声明；曲库使用任务实际生成的媒体 URL，不自行猜测格式。
- 离线预设曲目的试听通过本地服务读取游戏已下载的 `songStoragePath/<nameKey>/<nameKey>_TOD1200/1730/2000_NI_L.mp4`，优先 TOD1730，支持 GET/HEAD/单段 Range，不再依赖容易过期的远程鉴权 URL。前端预设试听地址使用 `http://localhost:27149/toy/music/preview/<nameKey>`（端口随接入地址配置）；原生演奏仍使用原有 `videoUrl/videoByTodView` 契约。
- 试听是前端独立音频播放器，底部演奏栏只反映原生演奏，不随试听更新。这是当前交互边界，不把“底栏空白”单独判为试听失败；以实际发声、试听状态和错误日志共同验收。

## 设计

1. 曲库只消费 `finished` 任务；任务生命周期和输入恢复由 [Phase 4-1](./phase4-1-music-settings-and-media.md) 负责，不能在曲库层重复建立状态机。
2. 歌单保留原版 `itemType/itemId` 兼容字段，同时保存本地曲目名称、媒体 URL、时长和演奏类型；加入和移除操作通过网关统一转换 snake_case/camelCase 输入。
3. 模块入口以注册表元数据和用户配置为准。配置加载时拒绝未知模块和敏感字段；启动时解析具体 Renderer、PlaybackAdapter 和 Encoder，失败要返回可读错误。
4. 配置文件是当前开发版的选择入口；本里程碑不提前引入新的 GUI 或运行时改写 API。最终普通用户设置页属于 Phase 7，但本里程碑要保持配置字段和模块目录稳定可复用。
5. 更换编码器只影响后续生成的媒体。已完成任务在现有 `info_json` 中保存自己的扩展名、MIME 和编码器标识；旧版本任务依据原文件后缀恢复格式。任务查询、曲库和 HTTP 媒体读取使用该任务的格式，不能按服务当前配置重新标记旧文件；不新增数据库表或迁移框架。
6. 试听直接以文件流读取缓存，复用现有网关的 Range 计算规则：`bytes=-N` 返回末尾 N 字节，开放末端和越界末端正确截取，无效范围返回 416。空文件或缺失缓存返回 404；读取失败关闭当前响应，客户端断开后释放文件流，避免缓存盘不可读时出现未处理的流错误。

## 验收标准

1. 一个已完成上传任务能在 `/toy/searchUserSongs` 出现，失败、取消和处理中任务不会出现；分页总数和游标稳定。
2. 用户曲目能通过原版加入歌单接口保存，重复加入不产生重复项，移除后查询不到；服务重启后歌单仍存在。
3. WAV/MP4 曲目在曲库和歌单中沿用任务声明的扩展名、媒体 URL 和 MIME 契约。
4. `user-config.json` 能选择已注册 Renderer、PlaybackAdapter 和 Encoder；未知选择、敏感字段和无效配置会被拒绝。
5. 模块选择变更在本地服务重启后生效，不影响信件模块和已完成的 Phase 3 链路。
6. 预设试听能从本地曲库缓存读取并支持浏览器 Range 请求；缓存不存在时返回明确的 404，不暴露任意路径。
7. `pnpm test`、网关兼容测试和 `git diff --check` 通过；不把配置文件验收当作 Steam 实机播放验收。

## 自动化验收记录

- 已有测试覆盖用户曲目分页、失败任务过滤、歌单增删、重启后的 SQLite 持久化、WAV/MP4 媒体契约和模块选择解析。
- 2026-09-09 通过真实 `createLocalApp` 启动入口复现并修复：原版客户端只提交 `itemType/itemId` 时，历史 ID 歌单会缺少名称、媒体元数据，且 ISO 时间会被原版按 Unix 秒解析为 `Invalid Date`。现复用已有 `MidiJobService` 曲目适配器和歌单存储逻辑补齐字段；已有完整名称的重复加入仍保持第一次保存的名称。
- 新测试通过真实 HTTP 验证只提交 `itemType/itemId` 时的曲目补齐、历史 ID 记录回填、Unix 时间、重复加入、服务重启后读取及 snake_case 移除；慢生成任务在完成前不进入曲库。另验证 WAV/MP4 双向切换编码器后，旧媒体的后缀、MIME 和字节不变，新媒体采用新设置，并兼容缺少格式元数据的旧记录。
- 历史收尾的完整 `pnpm test` 为 **133/133 通过**，`git diff --check` 通过。新增预设缓存试听路径解析和 HTTP Range 测试；MP4 切换测试使用合成编码器验证协议和存储行为，不把它当作真实视频解码或 Steam 播放证据。收尾提交后的当前完整回归为 **143/143 通过**。
- 收尾检查用真实 HTTP 复现 `bytes=-5` 错误返回开头 6 字节；修复后验证末尾范围、开放末端、超长后缀、无效范围、HEAD、缺失与空缓存。不更换播放器或生成媒体，这项接口边界修复由自动化验收，不要求重复整套 Steam 测试。

## 用户操作路径与验收记录

| 验收点 | 结果与证据 |
| --- | --- |
| “我的上传”保留已生成曲目 | 用户重开客户端后确认 10 秒 MIDI 仍在；上传、生成和轮询已由 Phase 4-1/4-4 验收 |
| 加入、重复加入、移除歌单 | 用户截图确认加入成功且只出现一次；移除后歌单为空，上传曲目仍在；服务重启持久化由真实启动入口测试覆盖 |
| 歌单名称与演奏 | 修复缺失元数据后，用户确认显示原文件名，从歌单点击可进入黑屏演奏并有声音；原生媒体进度已由 Phase 4-4 验收 |
| 模块选择与重启生效 | 按[配置指南](./user-config.md#演奏)使用配置文件或终端向导；实际启动装配、无效选择拒绝、编码器双向切换和旧媒体格式通过自动化验证。没有要求用户寻找不存在的游戏内设置页 |
| 预设及上传曲目试听 | 用户先确认试听修复有效；预设地址随后改为 localhost，因此追加小范围复测。用户重启后确认古典、ACG、轻音乐及上传 MIDI 试听全部正常 |

2026-09-09 17:12–17:15（UTC+8）的 `Olivia.log` 核查窗口包含 6 次 `songPreviewStart`，已使用 localhost 预设试听地址，未出现 `Failed to play audio`、`Audio error`、`ERR_CONNECTION` 或 `ERR_SSL`。其中仍有 Mixed Content 警告；警告和开始埋点都不能单独证明播放成败，最终发声与不中断的结论来自用户实机确认。不提交完整私人日志或游戏媒体。

## 保留边界与后续工作

修复前记录（2026-09-11）：预设曲目加入歌单后会显示数字 ID、时长为零且无法演奏；上表此前的歌单演奏通过证据只覆盖上传 MIDI。0.0.9.627 的 `An` 只发送 `itemType/itemId`，本地服务没有原版曲库可用于补齐预设数据。`applyOfflinePresetPlaylistPatch` 复用客户端已有 `offlineCatalog`，在加入前和读取歌单后补齐预设的名称、`songId`、时长、`nameKey` 及原有视频/TOD 字段；读取时可恢复旧 ID 条目，再次加入会通过既有接口持久化，保留添加时间和去重语义。未知 ID、上传 MIDI、演奏收藏和在线模式沿用原处理。

修复前记录：补丁已纳入离线完整补丁及现有增量脚本；自动化覆盖冷启动目录加载、历史条目、重复加入、重启、移除和类型隔离。并行/串行完整复跑均 138/138 通过；首轮各有一个 `fetch failed: bad port`，相关用例单跑通过，未改测试端口或断言。实际 0.0.9.627 前端暂存副本的四处签名各命中一次，归档仅主脚本改变且通过语法检查。

## 2026-09-11 预设歌单收尾验收

提交 `f51d630` 将服务端 snake_case/camelCase 字段兼容、预设元数据持久化和歌单封面加载失败时的音符占位合并为一条补丁，并补齐旧数字条目的读取回填。收尾提交完成后，全量测试和定向测试分别为 `143/143`、`11/11`；真实 Steam `0.0.9.627` 的 `feapp.dat` 已在外置备份和 SHA-256 记录保护下应用，未修改 `version.json` 或 `NutStudioUI.dll`。

用户在补丁后完成 Steam 实机操作并确认最终验收通过：旧曲目和新加入预设曲目都显示正确名称与时长，封面缺失时显示音符占位，点击播放能进入演奏；重复加入和移除仍保持既有语义。该结论以用户实际观察为实机证据，HTTP 与脱敏日志仅作辅助，不扩展为全部预设缓存或其他客户端版本均已验证。

- 本轮是已接入的 Steam 0.0.9.627 功能验收，覆盖三类预设样本和歌单加入回归，未遍历全部缓存。缺失缓存、哈希文件名等不符合上述命名的资源不会被当前解析器自动识别；继续使用只读缓存适配，不盲猜文件对应关系。
- 第三方 Renderer/Encoder、其他游戏版本和严格 3D 表现不在本轮验收范围。底部演奏栏与试听的统一交互若以后扩展，应另写设计与验收。
- 原版安装执行器尚未整合全部离线 MIDI 增量补丁，不能把当前安装样本的成功等同于从原版一次安装成功。具体复现与 Phase 7 安装门槛见[游戏接入](./original-installation.md#当前交付边界2026-09-09)。
- 下一开发主题为 Phase 4-5 外部音乐导入：先复用检查、调研与设计，再进入实现；不重做已验收的播放器、曲库或任务系统。
