# Phase 2：游戏接入

## 已开始

- 版本白名单和未知版本拒绝。
- 客户端关键文件 SHA-256 基线校验。
- 变更状态可区分为 `missing`、`unchanged` 和 `changed`。
- 独立备份目录、备份清单和带完整性校验的回滚。
- 已在真实 0.0.9.627 游戏目录上完成只读 dry-run：版本识别正确，7 个关键文件全部 `unchanged`。
- 已建立前端压缩资源的补丁规划和构建链：先校验唯一 `main-*.js`、接口出现次数和补丁标记，再在内存构建输出包。
- 对当前安装目录的 `feapp.dat` 检查显示，11 个信件/歌单接口均已是本地 `/toy` 路由；规划器会把它们识别为 `local`，不会重复覆盖。

## 下一步

1. 在真实 `feapp.dat` 上继续核对离线开关、MIDI 上传和任务轮询接口。
2. 将现有本地路由与网关响应逐项做字段级联调。
3. 在真实游戏目录上执行 dry-run 和启动冒烟测试。

任何未知版本、基线不一致或备份失败都必须停止补丁流程。
